{% extends 'FreelancerToolsCoreBundle::layout.html.twig' %}

{% block body -%}
    <h1>PaymentMethod list</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Name</th>                
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('paymentmethod_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>                
                <td>
                <ul>
                    <li>
                        <a href="{{ path('paymentmethod_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('paymentmethod_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('paymentmethod_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
